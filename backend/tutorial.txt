# criar venv
python -m venv .venv

# Ativar venv:
.venv\Scripts\Activate

# Baixar requirements
pip install -r requirements.txt

# Executar o backend:
# OBS: deve estar na pasta backend e com o venv ativo
uvicorn app:app --reload --host 0.0.0.0 --port 8000